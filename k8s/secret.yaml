# =============================================================================
# Kubernetes Secrets - Sensitive Configuration
# =============================================================================
# Stores sensitive data like passwords and API keys
# NOTE: In production, use external secrets management (AWS Secrets Manager, 
#       HashiCorp Vault, or Kubernetes External Secrets)
# Values are base64 encoded. To encode: echo -n 'value' | base64
# Usage: kubectl apply -f secret.yaml -n <namespace>

apiVersion: v1
kind: Secret
metadata:
  name: image-processing-secrets
  labels:
    app: image-processing
type: Opaque
data:
  # Flask Secret Key (base64 encoded: 'super-secret-key-change-in-production')
  SECRET_KEY: c3VwZXItc2VjcmV0LWtleS1jaGFuZ2UtaW4tcHJvZHVjdGlvbg==
  
  # PostgreSQL Password (base64 encoded: 'postgres')
  POSTGRES_PASSWORD: cG9zdGdyZXM=
  
  # RabbitMQ Password (base64 encoded: 'guest')
  RABBITMQ_DEFAULT_PASS: Z3Vlc3Q=

# =============================================================================
# Production Secrets (apply separately to prod namespace)
# =============================================================================
# To apply production secrets:
# kubectl apply -f secret-prod.yaml -n image-processing-prod
